name: Deploy to Cloud Run

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      CLOUDSDK_CORE_PROJECT: ${{ secrets.GCP_PROJECT }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v1
        with:
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          project_id: ${{ secrets.GCP_PROJECT }}

      - name: Build image and push to Container Registry
        run: |
          IMAGE="gcr.io/${{ secrets.GCP_PROJECT }}/news-app:${{ github.sha }}"
          gcloud builds submit --tag "$IMAGE" --project="${{ secrets.GCP_PROJECT }}"

      - name: Deploy to Cloud Run
        run: |
          IMAGE="gcr.io/${{ secrets.GCP_PROJECT }}/news-app:${{ github.sha }}"
          gcloud run deploy news-app \
            --image "$IMAGE" \
            --region "${{ secrets.GCP_REGION }}" \
            --project "${{ secrets.GCP_PROJECT }}" \
            --platform managed \
            --allow-unauthenticated
